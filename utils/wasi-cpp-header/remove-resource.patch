--- a/typenames.witx
+++ b/typenames.witx
@@ -273,9 +273,8 @@
   )
 )
 
-(resource $fd)
 ;;; A file descriptor handle.
-(typename $fd (handle $fd))
+(typename $fd (handle))
 
 ;;; A region of memory for scatter/gather reads.
 (typename $iovec
